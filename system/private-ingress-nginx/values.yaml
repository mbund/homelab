ingress-nginx:
  controller:
    ingressClassResource:
      name: private-nginx
      enabled: true
      default: false
      controllerValue: "k8s.io/private-ingress-nginx"
    admissionWebhooks:
      timeoutSeconds: 30
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
    service:
      type: ClusterIP
      clusterIP: 10.43.0.60
